pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

//rootProject.name = "Template"
include(":android")
include(":android:app")
include(":shared")